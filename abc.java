package academy.learnprogramming;

import java.util.Scanner;

public class abc {
    public  int x = s.nextInt();;
    public static Scanner s = new Scanner(System.in);

    void X() {
        for (int x = 1; x <= 12; x++) {
            System.out.println(x + " times " + this.x + " equals " + this.x * x);
        }
    }
}
